import * as React from "react";
import {RouteProps} from "react-router";

export class AuthRoute extends React.PureComponent<RouteProps> {
  render() {
    return(
      <p>Auth Route</p>
    );
  };
};
